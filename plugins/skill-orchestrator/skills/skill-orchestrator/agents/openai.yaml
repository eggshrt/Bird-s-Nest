interface:
  display_name: "Skill Orchestrator"
  short_description: "Align requirements and run a recoverable multi-Agent DAG"
  default_prompt: "Use $skill-orchestrator to align my request, retrieve suitable skills, and run a recoverable V2 execution graph."
policy:
  allow_implicit_invocation: false
