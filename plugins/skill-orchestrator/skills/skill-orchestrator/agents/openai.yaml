interface:
  display_name: "Skill Orchestrator"
  short_description: "Align requirements, retrieve skills, and run a verified DAG"
  default_prompt: "Use $skill-orchestrator to align my request, retrieve suitable skills, and propose a safe execution plan."
policy:
  allow_implicit_invocation: false
