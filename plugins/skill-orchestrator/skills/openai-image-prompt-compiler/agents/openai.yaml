interface:
  display_name: "OpenAI Image Prompt Compiler"
  short_description: "Compiles one OpenAI-first Chinese prompt"
  default_prompt: "Use $openai-image-prompt-compiler only with a dispatcher-signed AgentTaskV1 packet."
policy:
  allow_implicit_invocation: false
