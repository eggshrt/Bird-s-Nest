interface:
  display_name: "Motif Curator"
  short_description: "Keeps only high-value visual motifs"
  default_prompt: "Use $motif-curator only with a dispatcher-signed AgentTaskV1 packet."
policy:
  allow_implicit_invocation: false
