interface:
  display_name: "Reference Role Director"
  short_description: "Limits what each reference may control"
  default_prompt: "Use $reference-role-director only with a dispatcher-signed AgentTaskV1 packet."
policy:
  allow_implicit_invocation: false
