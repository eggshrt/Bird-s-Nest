interface:
  display_name: "Synthesis Adjudicator"
  short_description: "Resolves expert conflicts into one recommendation"
  default_prompt: "Use $synthesis-adjudicator only with a dispatcher-signed AgentTaskV1 packet."
policy:
  allow_implicit_invocation: false
